# Pages

Registry of all pages in the prototype. Update this file whenever a page is added or its status changes.

| Page | Path | Section | Status | Notes |
|---|---|---|---|---|
| Order Summary | `/order-summary` | — | IN PROGRESS | Orders table populated with real sample ECF data (309 orders / 18 windows / 30 sessions), fresh/deleted/first-order row states, working dispatched/deleted filters. AU-only sessions: NZ doesn't use sessions, only windows, so for `isNZ` the table flattens each wave/session group's sub-groups (e.g. "5098 CDMD2" → "15:00-16:00") into standalone top-level "window" groups via `flattenWaveGroupsToWindows` (`orderGroups.ts`) — same underlying mock rows, no session wrapper row, hold-control reads "window" not "session". `onDemand`-kind groups are untouched for both countries. The header's "Session overview" drawer link/panel (`OverviewDrawers.tsx`) is also AU-only (`isAU`); "Window overview" shows for both. |
| Order Details | `/order/detail?id={orderNo}` | — | IN PROGRESS | Reached by clicking an order row. Grey toolbar with quick actions, order: Print Invoice for Dispatched Order — AU only, via `isAU`, opens a `[DEMO]` placeholder dialog (`PrintInvoiceDialog.tsx`); Print Packing Slip — NZ only, via `isNZ`, and only shown when the order's `packingSlipRequired` flag is set, mimicking Order Summary's bulk Packing Slips quick action; Print Order List — NZ only, via `isNZ` (AU has no equivalent need for this, hidden entirely); Manual Picking — real-world BCP fallback only, hidden behind a fake `showManualPicking` feature flag (off by default, both AU/NZ) toggled via a floating demo-only gear icon bottom-left (`DemoBcpManualPickingToggle.tsx`, mirrors Order Summary's `DemoWindowStateToggle` pattern); Move Order/Line from OSR to Shop Floor — OSR actions gated to eStore via `isEstore`. + Articles/Details/Instructions/Labels/Samples/Audit tabs + a far-right **Edit Details**/**Cancel** button (not a quick action — Details tab only, NZ only, Customer Support persona only). Global header swaps to order-number stepper (prev/next chevrons, proposition subline) on this route. **Details tab** built: 3-column card layout (Fulfilment / Customer / Payment & Fraud, each in a bordered `Card` with a section-title header + icon) with a Badge-pill Status dropdown, On Hold/Priority Order/Ignore Transit/Ignore Fraud Status checkboxes (always editable), and — only while the toolbar's Edit Details mode is on — editable Name/Mobile/Phone/Work Number/Delivery Address 1-3 (Customer Number stays read-only), Delivery Date (date-picker Popover+Calendar), Delivery Window (dropdown of 3-hour blocks), and Fulfilment Fee; all fields wired to a shared per-field yellow dirty highlight and a single tab-wide `isDirty`/`SaveDiscardBar`/`useNavigationGuard`/`UnsavedChangesDialog` pattern ("You have unsaved changes to Order Details."), all state lifted into `OrderDetails.tsx` via `useEditableField` so the toolbar button and the tab's save bar share one source of truth; NZ-only Confirmation Number field; one canonical dummy record shown for every order, real per-order variation still to come. **Instructions tab** built: Personal Shopper / Customer Care / Delivery instructions, first two editable while `suppliedStatus` is `notStarted`/`picking` (textarea, pale-yellow dirty highlight, bottom Save/Discard bar wired to the same shared pattern, "You have unsaved changes to Instructions."), read-only plain text with grey empty-state copy once `packed`/`atRisk`/`deleted`; Delivery Instructions always read-only. Articles/Labels/Samples/Audit tab content and popup content remain placeholders |
| Search Orders | `/search-orders` | — | IN PROGRESS | Reached via the header's Search icon link. Replicates the legacy ECF search screen: Filters form (Status, Order No/Transit code, Customer name, Customer number, Fraud reference, Article No/Barcode, Select date range preset + Beginning/Ending date pickers, "Search all stores" checkbox restricted to Support Office/Customer Support) over Search/Reset Filters buttons. Results table (`SearchResultRow.tsx`) is a flat, ungrouped list styled like Order Summary's `OrdersTable` (reuses `FLAG_COLUMNS`/`FLAG_ICONS`/`rowHighlightStyle` exported from there) with a grey toolbar (rounded corners matching the Labels tab's table, print-invoices icon disabled until rows are selected) above it — clicking a row navigates to `/order/detail?id={orderNo}` exactly like Order Summary. Adds two columns not on Order Summary: a leading **Session/Window** column (AU shows the order's pick session e.g. "5098 AM", NZ shows its window e.g. "BB" — via `getOrderSessionAndWindow` in `orderGroups.ts`) and **Pickup Date** (right after the checkbox column, sourced from Order Details' `deliveryDate` — currently the same canonical dummy date for every order, so it merges across all rows until per-order dates exist). Supplied %/Total Order Supply is omitted (superseded by the two new columns). Session/Window, Pickup Date, and Customer cells merge (rowSpan) across consecutive rows sharing the same value, via `computeMergeSpans` (`mergeSpans.ts`). Searches the same 309-order `orderGroups.ts` mock dataset; only Status, Order No/Transit code, Customer name, and a fraud-flag check on Fraud reference actually filter — Customer number, Article No/Barcode, and the date range are rendered for visual fidelity but have no backing field on `OrderRow` to filter against. No results table shown until Search is clicked, matching the legacy screen. |
| Dash (Real Time Performance) | `/dash` | — | IN PROGRESS | Reached via the header's "Dash" nav link (now wired to a route instead of a placeholder `href="#"`). Grey toolbar with centered Indicators/Timeline tabs (same shadcn Tabs pattern as Order Details). Global header subline swaps to "Last refreshed HH:MM:SS" + a Refresh link on this route instead of the static page-load time. **Indicators tab**: 6-tile grid — custom SVG semicircle `Gauge` component (no charting library in this repo) for Items Per Labour Hour and RF Pick Rate (+ 4 zone mini-gauges in a 2x2 grid), each gauge coloured pale/bright-red below target and light green at/above target with a blue target tick, needle allowed to overshoot slightly past max so out-of-range values (e.g. RF Pick Rate > 200) are visible without being clamped; Estimated Picking End Time and Count of Trucks as plain tables; Count of Team Members as a simple bar chart and Count of Totes as a stacked bar chart using the exact Packed/Picking/Awaiting colours from the Order Summary supply bar. **Timeline tab**: "Personal Shopper Timeline" Gantt-style table (reuses `StickyTableContainer`'s sticky-header helpers) — one row per picker with a zone-coloured shift bar (hover shows zone + duration via `Tooltip`) and three metric cells colour-coded by % of target (pale red <95%, pale yellow 95-99% matching the app's dirty-field highlight, light green 100%+ matching the Picking supply-bar colour). Picking-zone vocabulary (Ambient/Chilled/Frozen/Security) reused from `StatPanel.tsx` for consistency with Order Summary. All data mocked in `dashData.ts` (seeded PRNG, static across renders). No persona/store gating (open question — flagged for follow-up). |
| Settings | `/settings` | — | SCAFFOLD | Reached via the header's Settings gear icon (now wired to a route instead of a placeholder `href="#"`). Placeholder copy only: "Will load Admin Console MFE" — this page will eventually host/embed the Admin Console micro-frontend. |
| Reports | `/reports` | — | SCAFFOLD | Reached via the header's Reports icon (now wired to a route instead of a placeholder `href="#"`). Placeholder copy only, flagging an open design question: "TBD approach for reporting - potential options include embed current state ECF/EARL functionality here or deep link back to ECF/EARL". |

## Status key
- **SCAFFOLD** — route exists, no real content yet
- **IN PROGRESS** — partial content, still being designed
- **DONE** — fully designed and interactive

## Adding a New Page
1. Create `src/pages/MyPage.tsx`
2. Add the route to `src/App.tsx` inside the AppShell `<Route>`
3. Add the nav item to `src/data/navigation.ts`
4. Add a row to this table
